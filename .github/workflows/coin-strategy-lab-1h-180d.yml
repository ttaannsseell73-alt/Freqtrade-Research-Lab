name: coin-strategy-lab-1h-180d

on:
  push:
    branches:
      - foundation/coin-strategy-lab-v1
    paths:
      - .github/workflows/coin-strategy-lab-1h-180d.yml
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e .
      - name: Run all-futures 1h 180d validation
        run: |
          python scripts/run_coin_strategy_lab_discovery.py \
            --start 2026-03-28 \
            --end 2026-09-24 \
            --timeframe 1h \
            --data-source vision \
            --workers 24 \
            --output-dir artifacts/coin-strategy-lab-1h-180d
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coin-strategy-lab-1h-180d
          path: artifacts/coin-strategy-lab-1h-180d
          if-no-files-found: warn
          retention-days: 30
